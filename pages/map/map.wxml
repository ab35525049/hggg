<view class='go_box'>
  <view>{{map_t}}</view>
  <view bindtap='go_tap' class='go_tap'>{{map_diss}}</view>
</view>
<view class='map_v'>
  <map id="map" wx:if="{{!(map_dis==1)}}"  scale="13" show-compass  latitude="{{laa}}" longitude="{{lnn}}" bindcontroltap="controltap" markers="{{markers}}" bindmarkertap="markertap" controls="{{controls}}" bindregionchange="regionchange" show-location style="width: 100%; height: 500px;"></map>

  <!-- controls="{{controls}}" -->
  <block wx:if='{{map_dis==1}}'>
    <view class='map_box'>
    <view class='map_box_'>
      <block wx:for="{{map_}}" wx:for-index="m_index" wx:for-item="m_item">
        <view>
          <view bindtap='m_tap' data-name='{{m_index}}' class="{{mp==m_index?'t':'f'}}">{{m_item.storename}}</view>
        </view>
      </block>
      </view>
      <button bindtap='go_tap_'>导航</button>
    </view>
  </block>


</view>